zerops:
  - setup: nodejs0
    build:
      base: nodejs@22
      buildCommands:
        - npm i
      deployFiles: backend/~
      cache: backend/~
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      start: npm start
