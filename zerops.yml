zerops:
  - setup: nodejs0
    build:
      base: nodejs@22
      buildCommands:
        - npm i
      deployFiles: backend/~
      cache: backend/~
    run:
      base: nodejs@22
      ports:
        - port: 4001
          httpSupport: true
      start: npm start
